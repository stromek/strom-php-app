// import {} from "./control.actions.js";

const getInitialState = function() {
  return {

  }
};

export default function(state = getInitialState(), {type, payload}) {
  // switch(type) {
  //   case '':
  //     return {...state};
  // }

  return state;
}